#!/bin/bash
# ============================================================
# 04_stop.sh — 서비스 중지
# ============================================================
DEPLOY_DIR="$(cd "$(dirname "$0")/.." && pwd)"
cd "$DEPLOY_DIR"

echo "=== SR DataLake 서비스 중지 ==="
docker compose down
echo "  → 중지 완료 (데이터 볼륨은 유지됨)"
