# HANDOFF — 2026-08-10 23:10 KST

illam · main @ 7658ec4 · origin 과 0/0

## 지금

레거시 시뮬레이터(FDC/MES/VMS) 수집을 `sr_ai_agent/sim/README.md` 설계대로 재구축했다.
커넥터를 시스템 단위 3개로 나누고 파이프라인을 태그별 8개로 분리해, DGX 데이터레이크에
소스 테이블 단위로 **8개 테이블 3,393행**을 적재 완료. 소스 행수와 정확히 일치하며
README 의 인과 정답(EQ01 불량률 46.7% / VIB 12건, e00001 임계초과 46회)도 재현된다.

백필 도중 MQTT 유실이 발생해 브로커 송신 큐를 3개 환경 모두 상향(1000 → 100000)했다.
근본 원인인 sink 성능(메시지마다 PG 커넥션 신규 생성, 건당 7ms ≈ 144건/s)은 미해결로 남았다.

## 기대값 — 시작 시 이 값과 대조한다

| 항목                 | 기대                                       |
|----------------------|--------------------------------------------|
| 스테이징 HEAD        | origin/main 과 0/0                         |
| DGX 배포 이미지      | sdl-app:latest · 2026-08-10 09:53 UTC      |
| 프로덕션 배포 이미지 | sdl-app:latest · 2026-08-10 09:51 UTC      |
| DGX 커넥터           | 3개 running · 에러 0 (FDC/MES/VMS)         |
| DGX 파이프라인       | 9개 running (신규 8 + 기존 OPC-UA 1)       |
| DGX 적재             | 8개 테이블 · 3,393행                       |
| 브로커 max_queued    | 3개 환경 모두 100000                       |

## 재확인

    scripts/verify-handoff.sh dgx

## 다음

1. **sink 커넥션 풀링** — `backend/services/pipeline_modules.py`.
   유실의 근본 해결. 현재 건당 7ms 로 버스트를 못 따라간다.
2. **프로덕션 수집이 전면 정지 상태**다. 파이프라인 0 · 커넥터 0 running.
   의도된 것인지 확인 필요.
3. **DGX 를 반입형으로 통일**. 지금은 자체 빌드·자체 커밋 중이라
   어느 코드가 도는지 커밋 해시로 확인되지 않는다.
4. 08-10 오전 introspection 변경분의 **프로덕션 배포 미실행**.

## 주의

- 리포의 `deploy/config/` 는 스테이징·프로덕션 실행 설정이 **아니다.**
  실행본은 스테이징 `~/sdl_deploy_package/config/`, 프로덕션 `~/sdl_keti_deploy/config/`.
- Benthos 는 컨테이너 재배포 시 복원되지 않는다. 커넥터 status 는 running 으로
  남고 수집만 멈춘다. 배포 후 `/ready` 를 반드시 확인할 것.
- 열려 있는 스크래치 슬롯: **없음.** `working_temp/` 는 비어 있는 것이 정상이며,
  비어 있지 않다면 되돌려 보내지 않은 편집이 있다는 뜻이므로 여기에 적혀 있어야 한다.
