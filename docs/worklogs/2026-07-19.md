# SR DataLake — 2026-07-19 작업 로그

## 요약
데이터 카탈로그 발견·다운로드 UX 개선 + 내부 스토리지 정합성 수정. origin/main
`272c90b` → `a7363e3` (13 커밋). 3서버(스테이징/프로덕션/DGX) 전부 배포·검증.

빌드/배포는 스테이징에서: `~/sdl-build` 빌드 → `docker save`/`scp`(맥 경유) → 프로덕션
`docker load`, DGX 는 `git pull` 후 재빌드. 프로덕션 롤백 태그 sdl-app:rollback-20260719[b~l].

---

## Ⅰ. 내부 스토리지 정합성

### d1cb90b — 내부 TSDB/RDB 자격증명 단일화
- 자격증명 출처를 `config.DB_*`(DATABASE_URL 파싱) 단일화. 하드코딩 폴백
  (`or "sdl_password_2025"`, `or "sdl_user"`) 6곳 제거.
- 원인: tenant 1 default TsdbConfig/RdbmsConfig 가 password="" 로 시드되어,
  쓰기(하드코딩 폴백 성공)/읽기(fe_sendauth 실패) 비대칭. 카탈로그 RDBMS 조회·
  TSDB SQL·export 가 실패했었다.
- `_migrate_fill_internal_storage_credentials` 백필 (tenant 1 한정 — tenant N 은
  t_N_user 격리 유지).

### eb56b44 — config_version 마이그레이션 누락 보정
- e6ad44e 가 pipeline/opcua/modbus/mqtt_connector 에 config_version 추가하며
  _migrate_add_columns 를 빠뜨림 → 기존 DB(스테이징)에 신규 이미지 올리자 500.
- 교훈: 모델 컬럼 추가 = _migrate_add_columns 동반 필수.

### 0b48714 — tenant1 TSDB database_name sdl_tsdb→sdl
- 폐기된 "별도 TSDB DB(sdl_tsdb)" 설계 잔재. self-heal 마이그레이션 + tenant_pg
  get_pg_info 표시값 교정. 스테이징/프로덕션 즉효 SQL + 3서버 배포.

### 0a42fcf — docs/tenant-schema-design.md
- 내부 TSDB/RDB 멀티테넌트 2단계 격리 설계 문서화(앱메타=public 행격리 /
  데이터=tenant_N 물리격리).

---

## Ⅱ. 관계형 DB 관리 — 신규 테이블 생성 (7393f31)
- 시스템 스토리지 > 관계형 DB 관리 테이블 목록에 "신규 테이블 생성" 버튼(관리자).
- `POST /api/storage/rdbms/<id>/tables`: 테넌트 스코프, 식별자 정규식 검증 +
  SYSTEM_PROTECTED_TABLES 금지 + 타입 화이트리스트 17종, psycopg2 sql.Identifier
  안전 quoting, 단일/복합 PK·NOT NULL·IF NOT EXISTS·COMMENT. PG 전용.

---

## Ⅲ. 카탈로그 — 커넥터 계층 트리
### 76ee9b9 / 5cd24e4
- 커넥터 노드에 raw id 대신 실제 이름 표시 (`/tree` 응답 {ct:{ci:{name,items}}} 확장,
  get_connector_name 신설: 실커넥터 6종 .name, pipeline 은 Pipeline.name, import 등
  그룹 카탈로그 connector_description/name 폴백).
- 트리 검색 필터(클라이언트) — 매칭 브랜치 자동 펼침 + 하이라이트.
- leaf 아이콘 태그(fa-tag)/그룹(fa-layer-group) 구분.

---

## Ⅳ. 카탈로그 — 목록 (소비자 우선)
### 36b6194 (Tier 1)
- 행 액션에 "데이터 조회"(주) + "다운로드/내보내기" — 상세 경유 없이 바로.
- 수정/삭제는 관리자만({% if is_admin %}).
- '민감도' 컬럼 → '유형'(sink 타입 배지 + 태그/그룹 스코프 아이콘).
- "공개된 것만" 토글.

### c41bf3a (Tier 2)
- 정렬(sort/order 파라미터 + 헤더 클릭, name/connector_type/category/data_level/updated_at).
- 검색 확장: owner + CatalogSearchTag 합류 + <mark> 하이라이트.
- 트리↔목록 연동: 커넥터 노드 클릭 → connector_id 필터 + 해제 칩.
- 페이지 크기(15/50/100).

---

## Ⅴ. 카탈로그 — 데이터 조회/다운로드
### a27aae9 — RDBMS 컬럼 필터를 다운로드에도 적용
- 조회 화면의 컬럼 필터(칩)가 다운로드/견적에 반영 안 되던 버그. 실측: 조회 171,232
  vs export 1,198,619(전체). `_build_rdbms_filter_conditions` 공유 헬퍼로 조회=export
  일치. 동기 스트림·preview·비동기(DatasetRequest.column_filters) 전부.

### 19eaba2 / c386c0c — 파일 폴더 단위(재귀) ZIP
- download-zip 에 folder(prefix) 모드: 하위 포함 재귀, base_prefix 상대경로로 구조
  보존(basename 평탄화 → 충돌 해소). 1GB/5000개 가드(413). 폴더 행 + "현재 폴더 전체"
  버튼. _catalog_file_base 헬퍼 추출.
- c386c0c: 헬퍼가 /files 라우트 데코레이터를 가로채던 500 fix.

### 684f13b / 188c2fe — async export 웹 + SFTP 다운로드 병행
- async 압축 결과물(MinIO)을 웹 또는 SFTP 로 수령. DatasetRequest.to_dict 에
  storageBucket 노출 → SFTP 경로 /{bucket}/{fileName}. 상태 모달·이력에 SFTP 버튼
  (/api/tenant/me/sftp-credentials 로 host/port/user/note, tenant-admin 이면 비번 재발급).
- SFTP 테넌트 격리는 기존 minio_iam.py(Phase 8 B-1) 재사용. 접근: 프로덕션/스테이징=
  SSH 터널, DGX=직접(8022). 기본 안내문에 두 방식 자체 설명.

### 3689c70 / 04d1291 / a7363e3 — 대용량 폴더 async ZIP + 다운로드 fix
- 동기 폴더 ZIP 이 1GB 초과 413 일 때 백그라운드 압축 경로.
  DatasetRequest.file_prefix + catalog_export_worker._execute_folder_zip
  (os.pipe→zipfile 스트리밍→MinIO multipart, 구조 보존). 프론트 413→"백그라운드
  압축" 모달→async→상태 모달(웹/SFTP).
- export 다운로드 Content-Disposition 비-ASCII(한글/특수문자) 파일명 fix
  (RFC 6266 + 고정 ASCII 허용셋 — str.isalnum() 이 한글을 남기던 문제). row export
  에도 있던 잠재 버그.

---

## Ⅵ. 알려진 잔여 / 후속
- pipeline 타입 커넥터 노드: 대상 삭제된 고아 카탈로그는 #id 폴백(정상).
- 동기 폴더 ZIP in-memory 한계 1GB — 그 이상은 async 로 유도(구현됨).
- 배포 정책: 스테이징 canonical, 프로덕션은 tarball, DGX 재빌드.

## Ⅶ. 참조
- Git HEAD: main = a7363e3
- 저장소(스테이징): ~/Workspace/sr_datalake (origin/main 추종)
- 커밋 저자: softrain (admin@softrain.co.kr)

---

## Ⅷ. 추가 수정 (2026-07-19 후반, a7363e3 이후)

### 499fa28 — DatasetRequest request_id 전역 유니크 충돌
- request_id(ds-YYYYMMDD-NNN)는 전역 유니크인데 시퀀스를 filter_by_tenant(테넌트
  스코프)로 계산 → 다른 테넌트(dd3=tenant4)가 같은 번호 재생성해 UniqueViolation
  (프로덕션 대용량 압축 에러). export async / 폴더 zip async / 대량 export 3곳을
  공유 헬퍼 _create_dataset_request(전역 max 시퀀스 + inject_tenant + 충돌 재시도)로 통일.

### 48367b6 / 6f66cee — 미정의 CSS 변수로 버튼 "안 보이지만 클릭됨"
- catalog.html 이 var(--info)/var(--primary) 등 미정의 변수 사용 → background 무효 +
  color:#fff → 흰 바탕 흰색. 내보내기 버튼(목록 아이콘·상세 버튼)이 대표 사례.
- 48367b6: catalog.html 을 정본 --c-* 로 교정. 6f66cee: style.css :root 에 전역 별칭
  (--primary→--c-primary, --info→--c-info, --bg-secondary→--body-bg 등) 추가해 앱 전반
  (collector/dashboard/builder/file_storage 등) 일괄 교정. 전 앱 미정의 var 0.

### 8f86964 — 폴더 async ZIP data descriptor 문제
- os.pipe(비-seekable) 스트리밍 zip 은 zipfile 이 data descriptor(플래그 bit3)를 써서
  local header 크기·CRC=0 → Windows 탐색기·macOS 아카이브 유틸이 "폴더만 생기고 파일
  없음"으로 오풀림(600MB zip 사례). seekable 임시파일에 zip 생성 후 fput_object 업로드로
  전환(정상 header, 모든 도구 호환). 동기 폴더 ZIP(BytesIO)·gzip 행 export 는 무관.

## 최종 HEAD
main = 8f86964. 3서버 배포·검증 완료. 프로덕션 롤백 태그 rollback-20260719[b~p].
