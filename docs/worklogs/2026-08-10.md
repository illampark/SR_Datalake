# SR DataLake — 2026-08-10 작업 로그

## 요약
DB 커넥터에 **소스 테이블 컬럼 코멘트(주석) introspection** 기능 추가. 기존에는 DB
커넥터가 `SELECT *` 로 데이터 행만 수집하고 컬럼 스키마/코멘트는 전혀 가져오지 않았음.
이제 테이블 등록 시 컬럼명·타입·NULL여부·PK·코멘트를 조회·저장하고, 데이터 카탈로그
`schema_info` 로 노출한다. MySQL/MariaDB · PostgreSQL 지원.

## 변경 파일 (5)
- `backend/services/benthos_manager.py` — `introspect_db_columns()` 신설.
  - MySQL/MariaDB: `information_schema.COLUMNS` (COLUMN_TYPE, IS_NULLABLE, COLUMN_KEY, COLUMN_COMMENT).
  - PostgreSQL: `information_schema.columns` + `col_description(regclass, ordinal_position)` +
    PK 조인. schema 는 `connector.schema_name` 없으면 `public`.
  - 반환: `[{name,type,nullable,comment,isPrimary}]`. Oracle/MSSQL 은 명시적 미지원 메시지.
- `backend/routes/collector_db.py`
  - `POST /api/connectors/db/columns` — 폼 자격증명으로 조회(신규 등록 화면, 저장 전).
  - `POST /api/connectors/db/<id>/columns` — 저장된 자격증명으로 조회(수정 화면, 비번 재입력 불필요).
  - `_normalize_tables_input` 이 `columns` 배열 보존(`_normalize_columns` 신설) → `config.tables[].columns` 저장.
  - update 시 `sync_connector_schema_info` 로 카탈로그 schema_info 갱신.
- `backend/services/catalog_sync.py` — `build_schema_info_from_tables()` + `sync_connector_schema_info()`.
- `backend/services/metadata_tracker.py` — `_auto_create_connector_catalog` 이 db 커넥터
  카탈로그 생성 시 config.tables 컬럼 스키마를 `schema_info` 로 초기화.
- `templates/collection/db_connector.html` — 테이블 행에 "컬럼 조회" 버튼 + 컬럼·주석
  표 렌더(`fetchColumns`/`renderColumns`). 저장 payload 에 columns 포함, 수정 화면 재현.

## 검증
- 5개 파일 `py_compile` 통과.
- PG introspection: DGX `sdl-postgres` 에 코멘트 포함 임시 테이블 생성 후, 수정한
  `introspect_db_columns` 를 **sdl-app 컨테이너 안에서 실제 실행** → 컬럼 3개 + 한글 코멘트
  + PK 정확히 반환 확인. 없는 테이블/미지원 DB 오류 경로도 확인.
- **발견·수정한 버그**: PG 쿼리의 `format('%I.%I', ...)` 리터럴 `%I` 가 psycopg2 파라미터
  치환과 충돌(`tuple index out of range`). `%%I.%%I` 로 이스케이프하여 해결. (psql 단독
  실행에선 드러나지 않고 psycopg2 경유에서만 발생 → 컨테이너 내 실제 실행으로 포착.)

## 스키마/마이그레이션
- 신규 컬럼/테이블 없음. 컬럼 스키마는 `DbConnector.config` (JSON) 에, 카탈로그 노출은
  기존 `DataCatalog.schema_info` (Text) 재사용 → **DB 마이그레이션 불필요.**

## 남은 작업 (배포)
- 코드는 스테이징 repo(정본)에 반영·검증 완료. **이미지 재빌드 + 3서버 배포는 미실행.**
  스테이징 canonical 빌드 → docker save/load(프로덕션) / git pull 후 재빌드(DGX) 절차 필요.
- MySQL 경로는 표준 `COLUMN_COMMENT` 쿼리로 저위험이나, 실 MySQL 소스로의 최종 확인은
  미실시(가용 MySQL 인스턴스·자격증명 없음).

---

## 저녁 세션 (18:40~23:10) — 레거시 수집 재구축 · MQTT 유실 대응 · 협업 규약 도입

### 진행

- **DGX 수집 복구.** 18:53 이미지 재빌드로 컨테이너가 교체되면서 Benthos 프로세스가
  함께 사라져 수집이 멈춰 있었다. 커넥터 restart 로 복구.
- **레거시 커넥터를 `sim/README.md` 설계대로 재구축.** 기존 성격 기준 2개 커넥터와
  파이프라인 2개, 카탈로그·태그메타·리니지·sink 테이블을 모두 삭제하고
  시스템 단위 3개(FDC/MES/VMS) + 태그별 파이프라인 8개로 재구성.
  컬럼 introspection 으로 한글 주석 35개 반영, 태그 8개에 식별자 체계 대응 관계 기술.
  → 데이터레이크 **8개 테이블 3,393행**, 소스 행수와 정확히 일치.
  README 인과 정답 재현 확인(EQ01 불량률 46.7% · VIB 12건 / e00001 임계초과 46회 · 최대 0.95).
- **MQTT 브로커 송신 큐 상향** — 3개 환경 모두 `max_queued_messages 100000`,
  `max_queued_bytes 52428800`. 자세한 근거는 아래 "서버 조작".
- **협업 규약 도입** — `HANDOFF.md`, `docs/runbook/environments.md`,
  `docs/deploy-log.md`, `docs/decisions/`, `scripts/verify-handoff.sh`,
  `scripts/local/wt.sh`, `CLAUDE.md` 신설.

### 서버 조작 (git 밖에서 한 일)

- **3환경 mosquitto.conf 큐 상향 + 재시작**
  - 근거: 백필 중 브로커 로그에 `Outgoing messages are being dropped for client
    sdl-pipeline-9` 확인(DGX 21:25:12). 프로덕션에는 과거 3회 발생 이력.
  - 산정: 메시지 평균 390B 실측 → 클라이언트당 약 39MB, 워커 4개 최악 156MB.
    가용 메모리 대비 DGX 0.13% / 스테이징 0.28% / 프로덕션 0.56%.
  - 백업: 각 서버 config 의 `*.bak.20260810_*`
  - **경로 주의**: 리포의 `deploy/config/` 는 실행 설정이 아니다. 처음에 이 착각으로
    스테이징 리포 파일을 고쳤다가 반영되지 않아, 실행본
    `~/sdl_deploy_package/config/` 에 다시 적용했다.
- **DGX 유령 Benthos 프로세스 3건 종료.** 커넥터 start 요청이 다른 gunicorn 워커로
  라우팅될 때마다 두 번째 인스턴스가 뜬다. 4195 를 점유하지 못해 스트림이 0개인 채
  살아 있어, 정상 인스턴스가 죽으면 조용히 수집이 끊길 수 있다.

### 발견

- **Benthos 는 컨테이너 재배포 시 복원되지 않는다.** `app.py` 에 기동 시 복원 로직이
  없고 `worker_reconciler` 는 로컬 워커와 파이프라인만 조정한다(Benthos 계열 제외).
  커넥터 status 는 `running` 으로 남아 DB 만 보면 정상으로 보인다.
- **`_benthos_proc` 가 gunicorn 워커별 전역 변수다.** `is_running()` 이 HTTP `/ready`
  보다 프로세스 핸들을 먼저 검사해, 살아 있어도 다른 워커에서는 False 를 반환한다.
  → restart 마다 유령 인스턴스가 생긴다.
- **`internal_rdbms_sink` 의 `flatten` 매핑은 소스에 `id` 컬럼이 있으면 깨진다.**
  sink 가 `CREATE TABLE (id SERIAL PRIMARY KEY, <소스컬럼>)` 을 만들기 때문에
  `column "id" specified more than once` 로 실패한다. FDC 두 테이블이 해당.
- **커넥터의 `batchSize` 설정은 무시된다.** customSql 에 `LIMIT 1000` 이 하드코딩돼
  있어 config 값이 적용되지 않는다.
- **sink 는 메시지마다 PG 커넥션을 새로 연다.** 실측 건당 7ms ≈ 144건/s.
  버스트가 mosquitto 기본 큐(1000)를 넘기면 QoS 1 이어도 드롭된다.
  단독·소량 버스트는 무손실(MES 140/140, VMS 565/565)이었고, 한 콜백이 두 파이프라인에
  2,000건을 동시에 밀어넣을 때만 유실됐다.
- **`flush_all_sink_buffers` 는 파이프라인 정지 시에만 호출된다.** 시간 기반 flush 가
  없어 batch 모드는 소량 테이블(결함코드 5행)이 영구히 버퍼에 갇힌다.
- **스테이징 `inpark` 은 sudo 그룹이지만 무암호 실행이 안 된다.** `sudo -l` 의
  `(ALL) NOPASSWD: ALL` 뒤에 오는 `(ALL : ALL) ALL` 규칙이 이긴다.
- **`server_info.txt` 의 프로덕션 비밀번호가 실제와 다르다.** 같은 사실이 로컬
  `server_info.txt` · 스테이징 `~/server_info.md` · 프로덕션 `~/BUILD.md` 세 곳에
  각기 다르게 낡아 있다 → `docs/runbook/environments.md` 로 단일화.

### 남은 작업

- **sink 커넥션 풀링** — 유실의 근본 해결. `pipeline_modules.py`.
- **프로덕션 수집이 전면 정지 상태** — 파이프라인 0 · 커넥터 0 running. 의도 확인 필요.
- **DGX 를 반입형으로 통일** — 현재 자체 빌드·자체 커밋이라 커밋 해시로 추적 불가.
- 오전 introspection 변경분의 **프로덕션 배포 미실행**.
- 커넥터 2(MES·VMS 스냅샷)는 upsert 가 없어 폴링마다 중복 적재된다.
  주기를 86400초로 늦춰 완화했으나 근본 해결은 아니다.

### 추가 발견 (규약 도입 중, verify-handoff.sh 첫 실행)

- **세 환경이 서로 다른 이미지를 돌고 있다.** 태그는 모두 `sdl-app:latest` 로 같지만
  이미지 ID 가 다르다 — 스테이징 `26622086141f`(09:50:34Z) · 프로덕션
  `477f11660f8b`(09:51:57Z) · DGX `bad1d980df80`(09:53:21Z).
  `docker save`/`load` 는 이미지 ID 와 빌드 시각을 보존하므로 반입이었다면 세 값이
  같아야 한다. `~/BUILD.md` 의 "프로덕션에서는 빌드하지 않는다" 정책과 어긋난다.
  → 한 번 빌드해 세 곳에 반출하는 흐름으로 통일 필요.
- **`docker inspect <컨테이너>` 의 `.Created` 는 이미지가 아니라 컨테이너 생성 시각이다.**
  "어느 빌드가 도는지" 를 보려면 `.Image`(이미지 ID)와
  `docker image inspect` 의 `.Created` 를 봐야 한다. 초안 스크립트가 이걸 혼동했다.
- **서버마다 docker 가 로컬시각/UTC 를 섞어 보고한다.** 정규화하지 않으면 환경 간
  비교에서 9시간을 오해한다. verify-handoff.sh 는 UTC 로 정규화한다.

### 정정 (2026-08-11) — 위 "추가 발견" 의 이미지 관련 결론은 틀렸다

`docker save`/`load` 가 이미지 ID 와 빌드 시각을 모두 보존한다고 전제했으나 **틀렸다.**
데몬이 로드 시 이미지를 재구성해 **ID 와 SIZE 가 호스트마다 달라진다**
(같은 이미지가 스테이징 715MB ↔ 프로덕션 960MB). 반면 이미지 config 의 `created`
는 보존된다.

이 기준으로 다시 대조하니 과거 태그 9개(`rollback-20260810`, `aasx`, `mqtt-asset`,
`mqtt-jsonpath`, `with-benthos-v1`, `phase8-storage-v1~v4`)의 빌드 시각이 스테이징과
프로덕션에서 **초 단위까지 일치**한다. → **반입 정책은 정상 작동해 왔다.**

- **DGX 가 따로 빌드하는 것은 정책 이탈이 아니라 필연이다.** DGX 는 `aarch64`,
  스테이징·프로덕션은 `x86_64` 다. 스테이징 이미지를 DGX 에서 실행할 수 없다.
  DGX 는 커밋 해시로 추적하며 롤백 태그는 `-dgx` 접미어로 구분한다.
- **DGX 에도 롤백 태그가 있다.** `rollback-20260810b-dgx`(18:20:10),
  `rollback-20260810-dgx`. "latest 를 덮어써서 되돌릴 지점이 없다" 는 서술은 틀렸다.
  스테이징도 `rollback-20260810b` 등을 보유한다. 세 환경 모두 태깅 관행이 있다.
- **환경 간 이미지 동일성은 빌드 시각(UTC)으로 판정해야 한다.** ID 는 호스트 안에서만
  유효하다. `verify-handoff.sh` 주석과 `environments.md` 에 반영했다.

남은 확인 사항: 오늘 프로덕션 이미지의 빌드 시각(09:51:57Z)이 스테이징(09:50:34Z)과
다르다. 과거 9개가 모두 일치했던 것과 대비되며, 프로덕션이 스테이징보다 83초 뒤라
단순 반입으로는 설명되지 않는다. 오늘 프로덕션에 올라간 것이 스테이징의 현재 빌드가
맞는지 확인이 필요하다.

---

## 2026-08-11 새벽 세션 — sink 커넥션 풀링 · 프로덕션 3건 조사 · DGX 카나리 배포

### 진행

- **sink 커넥션 풀링 + 스키마 캐시** (`3e12665`). 스테이징 측정 34배
  (건당 50.38ms → 1.49ms, 초당 20건 → 672건). 자기 치유 5항목 통과.
- **DGX 카나리 배포.** 롤백 태그 `rollback-20260811-dgx` 확보 후 빌드
  (`8e9d5b4f4b0e`) · 컨테이너 교체 · 복구 · 실데이터 검증.
- **유실 조건 재현 검증.** FDC 커서를 0으로 리셋해 한 콜백이 두 파이프라인에
  2,000건을 동시 발행하는 상황(= 08-10 유실이 났던 그 조건)을 그대로 만들었다.
  결과: 13초 만에 2,000건 전량 적재, 60초 뒤 나머지 688건, **총 2,688행 무손실.
  브로커 신규 드롭 0건.** 이전에는 같은 지점에서 temperature 가 42건만 들어오고
  구간 1~4 가 전멸했다.

### 조사 결과 (이전 세션의 미해결 3건 — 모두 조치 불필요로 종결)

- **프로덕션 이미지 빌드 시각 불일치 = 오탐.** 스테이징과 프로덕션 컨테이너의
  코드가 바이트 단위로 동일하다(benthos_manager · collector_db · catalog_sync ·
  pipeline_modules · db_connector.html 5개 해시 일치). 차이의 원인은 이미지
  저장소 방식이다 — 프로덕션 Docker 29.4.0 은 containerd 저장소(overlayfs),
  스테이징 29.6.0 은 클래식(overlay2). 같은 이미지도 ID·SIZE·created 가 달리 보인다.
  **이미지 ID·크기·빌드시각은 호스트 간 비교 지표로 쓸 수 없다.**
- **프로덕션 수집 정지 = 고객의 의도.** 감사 로그상 고객사 계정 dd3 가
  2026-07-10~07-15 에 걸쳐 직접 start/stop 을 반복한 뒤 정지 상태로 두었다
  (마지막 07-15 07:46 pipeline.stop 29·42). 장애가 아니다. 08-05 에 MQTT 커넥터
  ThingsBoard-PLC01 을 신규 생성했고 08-10 에도 로그인했다 — 활발히 사용 중이다.
- **08-10 오전 introspection 의 프로덕션 배포 = 이미 완료.** 프로덕션 컨테이너에
  `introspect_db_columns` 와 `/columns` 라우트 2개가 존재한다. 워크로그의
  "3서버 배포 미실행" 기록은 18:50~18:53 배포 이전에 쓰인 것이다.

### 발견

- **파이프라인 `/start` 는 DB status 가 running 이면 무효다.** 라우트가
  `if p.status == "running": return _ok({"message": "이미 실행 중입니다."})` 로
  엔진을 시작하지 않고 200 을 반환한다. 컨테이너 교체 직후에는 DB 는 running 인데
  워커에는 구독이 없으므로, `/start` 를 불러도 아무 일이 없고 성공처럼 보인다.
  실제로 이번에 9개 파이프라인에 `/start` 를 호출해 전부 200 을 받았지만 구독은
  0 이었고, 커넥터가 2,688건을 발행했는데 아무도 소비하지 않아 통째로 유실됐다.
  → **복구는 stop → start 로 해야 한다.** CLAUDE.md 배포 절에 반영했다.
- 배포 후 복구 순서는 **① 파이프라인 stop→start ② 커넥터 restart** 다.
  구독이 먼저 올라와야 커넥터의 첫 폴링 메시지를 놓치지 않는다.

### 남은 과제

- 파이프라인 자동 복구 부재는 구조적으로 남아 있다. reconciler 는 로컬에 뜬 것을
  정지·재로드만 하고 시작하지는 않는다(4 워커가 동시에 시작하면 중복 처리가 되므로).
  리더 선출 같은 별도 설계가 필요하다.
- MySQL 싱크는 스키마 캐시만 적용됐고 커넥션 풀은 없다.
- flatten 매핑의 `id` 컬럼 충돌은 그대로다(decisions/0001 참조).
