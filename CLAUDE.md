# SR Datalake — 작업 규약

## 작업 모델

- **원본은 스테이징 `~/Workspace/sr_datalake` 하나뿐이다.** 로컬에는 클론이 없다.
  로컬 `working_temp/` 는 왕복 편집용 스크래치다.
- 편집한 파일은 **즉시 되돌려 보내고 스테이징에서 커밋한다.** 로컬 파일이 어떤 변경의
  유일한 소재지가 되는 시간을 분 단위로 유지한다.
- git 조작(commit · push · log · diff)은 **전부 스테이징에서** 실행한다.
- 프로덕션에는 git 저장소가 없다. 이미지 tarball 을 받는다.

## 세션

**시작**
1. `HANDOFF.md` 를 읽는다 (지금 · 기대값 · 다음 · 열린 슬롯).
2. `scripts/verify-handoff.sh <env>` 로 기대값과 실제를 대조한다.
3. 어긋나면 `git log` 로 좁힌다. **상태 문서를 믿지 않는다.**

**종료**
1. `HANDOFF.md` 를 덮어쓴다. 열린 스크래치 슬롯이 있으면 명시한다.
2. `docs/worklogs/<오늘>.md` 에 섹션을 덧붙인다 (진행 · 서버 조작 · 발견).
3. 커밋 + push. 미완이어도 `wip:` 접두어로 push 한다.

## 기록

| 파일 | 담는 것 | 수명 |
|---|---|---|
| `HANDOFF.md` | 현재 상태 · 기대값 · 다음 | 매 세션 덮어쓰기 |
| `docs/worklogs/YYYY-MM-DD.md` | 한 일 · 서버 조작 · 발견 | 덧붙이기만 |
| `docs/runbook/environments.md` | 환경별 잘 안 변하는 사실 | 사실이 바뀔 때 |
| `docs/decisions/NNNN-*.md` | 결정과 **기각한 대안** | 영구 |
| `docs/deploy-log.md` | 어느 커밋이 언제 어디로 | 덧붙이기만 |

기대값 표에는 재확인 명령으로 **즉시 검증 가능한 항목만** 넣는다.
상태는 기록하지 말고 조회한다. 기록은 변하지 않는 것(결정 · 경위)에 남긴다.

## 스크래치

- 슬롯은 `<날짜>-<주제>` 로 만들고 `MANIFEST.tsv` 를 함께 둔다.
- **되돌려 보내기 전 서버 해시를 반드시 확인한다.** 다르면 덮어쓰지 않고 다시 받아
  편집을 옮긴다. 로컬에 git 이 없어 이 사고는 아무 흔적도 남기지 않는다.
- 되돌려 보낸 슬롯은 비운다. 남기면 다음 기기가 최신으로 오해한다.
- **평상시 `working_temp/` 는 비어 있다** (`_bin/` · `_done/` 제외).
  비어 있지 않다는 것은 되돌려 보내지 않은 편집이 있다는 뜻이며, 그 슬롯은
  `HANDOFF.md` 의 "열린 슬롯" 에 반드시 적혀 있어야 한다.
  **폴더가 비었는지 보는 것만으로 미완 작업 유무를 판단할 수 있게 유지한다.**
- `scripts/local/wt.sh` 가 위 절차를 강제한다.

## 배포

- **빌드는 스테이징에서만.** 프로덕션 · DGX 에서는 커밋도 빌드도 하지 않는다.
- 승격 단위는 커밋 해시다. "최신 main" 이 아니라 특정 해시를 빌드해 반출한다.
- 배포 전 직전 이미지에 `rollback-YYYYMMDD` 태그를 붙인다.
- 배포 후 `docs/deploy-log.md` 에 한 줄 추가한다. **검증 결과 포함.**
- 배포 후 점검은 "컨테이너가 떴는가"가 아니라 **"데이터가 흐르는가"** 를 본다.
- 리포의 `deploy/config/` 는 실행 설정이 아니다. 경로는
  `docs/runbook/environments.md` 를 확인한다.

## 금지

- 자격증명을 리포지토리에 넣지 않는다.
- 서버에서 일회성 명령을 직접 실행했다면 종료 전 `scripts/` 로 옮긴다.
